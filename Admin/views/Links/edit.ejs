<%- include('../layouts/Header'); %>
    <div class="row">
        <div class="col-lg-12 mx-auto">
            <div class="card">
                <div class="card-header py-3 bg-transparent">
                    <h5 class="mb-0">Edit Static Link</h5>
                </div>
                <div class="card-body">
                    <div class="border p-3 rounded">
                        <form class="row g-3" action="/links/edit" method="post">
                            <div class="col-12">
                                <label class="form-label">Link Name</label>
                                <input type="text" name="name" value="<%= links.name %>" class="form-control"
                                    placeholder="Enter Link Name">
                            </div>
                            <div class="col-12">
                                <label class="form-label">Course Year</label>
                                <input type="text" name="link" value="<%= links.link %>" class="form-control" placeholder="Enter  Link">
                            </div>
                            <div class="col-12">
                                <label class="form-label">Status</label>
                                <select name="status" class="form-select">
                                    <% if (links.status==true) { %>
                                        <option selected>Active</option>
                                        <option>Disabled</option>
                                        <% } else { %>
                                            <option>Active</option>
                                            <option selected>Disabled</option>
                                            <% } %>

                                </select>
                            </div>
                            <input type="text" value="<%= links._id %>" name="_id" hidden>
                            <div class="col-12">
                                <button type="submit" class="btn btn-primary px-4">Edit Link</button>
                            </div>
                            <span id="message" class="text-danger lead">
                                <%= messageEdit %>
                            </span>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div><!--end row-->
    <%- include('../layouts/Footer'); %>

        <script>
            setTimeout(() => {
                document.getElementById("message").innerHTML = ""
            }, 3000);
        </script>